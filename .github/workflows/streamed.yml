name: Update Sports Playlist

on:
  schedule:
    - cron: '*/30 * * * *' # Runs every 30 mins since we only want "Live Now"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install requests

      - name: Run Live Scraper
        run: python streamed_fetcher.py

      - name: Commit Files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add streamed.m3u
          git diff --quiet && git diff --staged --quiet || (git commit -m "Updated Live Now streams" && git push)
